# Static Site (Nginx)

Serves static files (HTML, CSS, JS, images) with Nginx.

## Usage

Place your website files in `.stack-data/site/` (mapped to Nginx's docroot).
`index.html` is served by default.

- **Endpoint:** `site` (primary) — protected by team membership
- **Resources:** 0.25 CPU / 64 MB RAM
